package controller;

import view.ChoiceStrategy;
import model.ListStrategies;
import java.io.IOException;

public class ToStrategy {

    public void selectStrategy (ChoiceStrategy choiceStrategy) throws IOException {

        if (choiceStrategy.selectPosition().equals(ListStrategies.FIRST_SPECIAL)){

        }

    }
}
